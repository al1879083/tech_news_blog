<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
        <span class = "meta">Posted by {{user.username}} on {{format_date created_at}}</span>
    </div>
        <p class="card-body">{{content}}</p>
</div>
